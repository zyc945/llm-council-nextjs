# ============================================
# LLM Council - Environment Configuration
# ============================================
# Copy this file to .env (for Docker) or .env.local (for local dev)
# and fill in your actual values

# ============================================
# Required Configuration
# ============================================

# OpenRouter API Key (必需)
# Get your API key at https://openrouter.ai/
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# ============================================
# Optional: Model Configuration
# ============================================

# Council Member Models (comma-separated)
# These models will participate in Stage 1 & 2
# Default: deepseek/deepseek-v3.2-exp,google/gemini-3-pro-preview,anthropic/claude-sonnet-4.5,x-ai/grok-4
COUNCIL_MODELS=deepseek/deepseek-v3.2-exp,google/gemini-3-pro-preview,anthropic/claude-sonnet-4.5,x-ai/grok-4

# Chairman Model (final synthesis in Stage 3)
# Default: openai/gpt-4o
CHAIRMAN_MODEL=openai/gpt-4o

# ============================================
# Optional: Runtime Configuration
# ============================================

# Node Environment (development | production | test)
# Default: production
NODE_ENV=production

# Server Port
# Default: 3000
PORT=3000

# Server Hostname
# Default: 0.0.0.0 (listens on all interfaces)
HOSTNAME=0.0.0.0

# ============================================
# Optional: Logging Configuration
# ============================================

# Log Level (error | warn | info | debug)
# Default: info
LOG_LEVEL=info

# ============================================
# Optional: Docker Configuration
# ============================================

# Data Directory (for docker-compose volume mounting)
# Default: ./data
DATA_DIR=./data

# Build Metadata (set by CI/CD)
BUILD_TIME=
GIT_COMMIT=

# ============================================
# Notes
# ============================================
#
# 1. Environment Files Priority:
#    - .env.local (local dev override, highest priority)
#    - .env.development (development environment)
#    - .env.test (test environment)
#    - .env.production (production environment)
#    - .env (default for Docker)
#
# 2. Security:
#    - Never commit .env* files with real API keys to git
#    - Only env.example should be committed
#
# 3. Model Selection:
#    - View all available models at https://openrouter.ai/models
#    - Models can also be configured per-conversation in the UI
#
# 4. Docker Usage:
#    - Production: docker-compose up -d
#    - Development: docker-compose -f docker-compose.dev.yml up
#    - Test: docker-compose -f docker-compose.test.yml up